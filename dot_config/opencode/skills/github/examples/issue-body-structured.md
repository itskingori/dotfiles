Documents and aligns GitHub skill workflows for PRs, issues, and comments using `gh` command patterns that are deterministic and scriptable.

### Scope
- Add explicit authentication and repository-context checks.
- Add input mode guidance for `--body`, `--body-file`, and heredoc usage.
- Add issue, PR, and comment workflow examples.

### Expected Outcome
- Faster, more consistent GitHub operations from the CLI.
- Fewer formatting regressions in multi-line markdown content.

### References
- https://cli.github.com/manual/
